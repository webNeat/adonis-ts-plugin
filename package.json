{
  "name": "adonis-ts-plugin",
  "description": "A Typescript plugin for additional type-checking for Adonis projects",
  "author": "Amine Ben hammou",
  "version": "1.0.0",
  "license": "MIT",
  "main": "./out/index.js",
  "files": [
    "out",
    "bin"
  ],
  "bin": "bin/adonis-ts-plugin",
  "scripts": {
    "build": "rm -rf out && tsc",
    "test": "npm run build && cd tests && npm install && npm test"
  },
  "devDependencies": {
    "@types/node": "^20.11.19",
    "tsx": "^4.7.1",
    "typescript": "^5.3.3"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
